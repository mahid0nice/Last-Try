#include<iostream>
#include<cmath>
using namespace std;

int main(){
   double x = 3;
   double y = 6;
   //z = max(x,y);
   //w = min(x,y);
   //double z= pow(2,3);
   //double w = sqrt(36);
   //double z = round(5.66);
   double z = ceil(3.14);
   double w = floor(3.14);

   cout<<z<<endl;
   cout<<w<<endl;

   return 0;
   
}